# Home Assistant

Version 3 uses the `custom_components/nwr_sdr` integration as its supported Home Assistant interface. The legacy YAML bridge was removed because it duplicated events and could not safely represent overlapping alerts.
